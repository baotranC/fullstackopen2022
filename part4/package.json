{
	"name": "bloglist",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "cross-env NODE_ENV=production node index.js",
		"dev": "cross-env NODE_ENV=development nodemon index.js",
		"lint": "eslint .",
		"test": "cross-env NODE_ENV=test jest --verbose --runInBand --forceExit --colors"
	},
	"author": "baotranC",
	"license": "MIT",
	"dependencies": {
		"bcrypt": "^5.1.0",
		"cors": "^2.8.5",
		"cross-env": "^7.0.3",
		"dotenv": "^16.0.3",
		"express": "^4.18.1",
		"mongoose": "^6.6.4",
		"mongoose-unique-validator": "^3.1.0",
		"morgan": "^1.10.0",
		"nodemon": "^2.0.20"
	},
	"devDependencies": {
		"eslint": "^8.27.0",
		"jest": "^29.3.1",
		"supertest": "^6.3.1"
	},
	"jest": {
		"testEnvironment": "node"
	}
}
